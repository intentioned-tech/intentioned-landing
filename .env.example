# =============================================================================
# INTENTIONED LANDING - CLOUDFLARE PAGES ENVIRONMENT VARIABLES
# =============================================================================
# These variables are set in the Cloudflare Dashboard, NOT in a local .env file
# Go to: Cloudflare Dashboard > Pages > Your Project > Settings > Environment Variables
#
# For local development with wrangler, create a .dev.vars file (gitignored)

# OpenAI API (for AI features via Cloudflare Functions)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: Discord Webhook for contact form notifications
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Optional: Newsletter service API key
# BUTTONDOWN_API_KEY=your-buttondown-key
# MAILCHIMP_API_KEY=your-mailchimp-key
